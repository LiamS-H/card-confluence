{
  "name": "@packages/wasm-browser",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:parallel": "RUSTFLAGS='-C target-feature=+atomics,+bulk-memory,+mutable-globals' wasm-pack build --target web --out-dir dist --out-name wasm_browser",
    "build": "RUSTFLAGS='-C target-feature=+bulk-memory' wasm-pack build --target web --out-dir dist --out-name wasm_browser"
  }
}
