[workspace]
members = [
    "packages/scryfall-rust-bindings",
]
# members = ["packages/*", "apps/*"]
resolver = "2"

[workspace.dependencies]
tokio = { version = "1.50.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
